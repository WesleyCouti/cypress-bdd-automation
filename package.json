{
  "name": "cypress-bdd-automation",
  "version": "1.0.0",
  "private": true,
  "description": "E2E test automation portfolio using Cypress, TypeScript, Cucumber/Gherkin and GitHub Actions.",
  "scripts": {
    "test": "cypress run",
    "test:chrome": "cypress run --browser chrome",
    "test:headed": "cypress run --headed",
    "open": "cypress open",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "latest",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "latest",
    "esbuild": "latest",
    "typescript": "latest"
  }
}
